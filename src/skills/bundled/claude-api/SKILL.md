# Claude API

Use this skill when the user wants to build with the Claude API or Anthropic SDKs.

## Reading Guide

Pick the language-specific docs that match the user's stack, then use the shared references for models, caching, errors, and tool use concepts.

## When to Use WebFetch

Use WebFetch when the user needs the latest official documentation or behavior that may have changed recently.
